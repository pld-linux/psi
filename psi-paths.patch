--- psi-0.8.7/src/main.cpp.orig	Wed Oct 30 11:09:34 2002
+++ psi-0.8.7/src/main.cpp	Tue Dec  3 20:35:22 2002
@@ -59,6 +59,9 @@
 	dirs += "." + subdir;
 	dirs += g.pathHome + subdir;
 	dirs += g.pathBase + subdir;
+	dirs += "/usr/share/psi/" + subdir;
+	dirs += "/usr/share/psi/translations/" + subdir;
+
 	for(QStringList::Iterator it = dirs.begin(); it != dirs.end(); ++it) {
 		if(!QFile::exists(*it))
 			continue;
@@ -125,6 +128,8 @@
 	dirs += "." + subdir;
 	dirs += g.pathHome + subdir;
 	dirs += g.pathBase + subdir;
+	dirs += "/usr/share/psi/" + subdir;
+	dirs += "/usr/share/psi/translations";
 
 	for(QStringList::Iterator it = dirs.begin(); it != dirs.end(); ++it) {
 		if(!QFile::exists(*it))
@@ -168,6 +173,8 @@
 	dirs.clear();
 	dirs += g.pathHome;
 	dirs += g.pathBase;
+	dirs += "/usr/lib/psi";
+	dirs += "/usr/lib/qt/plugins-mt/network";
 	dirs += "";
 	Stream::loadSSL(dirs);
 
