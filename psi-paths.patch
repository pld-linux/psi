diff -ru psi-0.8.7/src/main.cpp psi-0.8.7-AlchemyX/src/main.cpp
--- psi-0.8.7/src/main.cpp	Wed Oct 30 11:09:34 2002
+++ psi-0.8.7-AlchemyX/src/main.cpp	Fri Nov 29 23:08:02 2002
@@ -59,6 +59,9 @@
 	dirs += "." + subdir;
 	dirs += g.pathHome + subdir;
 	dirs += g.pathBase + subdir;
+	dirs += "/usr/X11R6/share/psi/" + subdir;
+	dirs += "/usr/X11R6/share/psi/translations/" + subdir;
+
 	for(QStringList::Iterator it = dirs.begin(); it != dirs.end(); ++it) {
 		if(!QFile::exists(*it))
 			continue;
@@ -125,6 +128,8 @@
 	dirs += "." + subdir;
 	dirs += g.pathHome + subdir;
 	dirs += g.pathBase + subdir;
+	dirs += "/usr/X11R6/share/psi/" + subdir;
+	dirs += "/usr/X11R6/share/psi/translations";
 
 	for(QStringList::Iterator it = dirs.begin(); it != dirs.end(); ++it) {
 		if(!QFile::exists(*it))
@@ -168,6 +173,7 @@
 	dirs.clear();
 	dirs += g.pathHome;
 	dirs += g.pathBase;
+	dirs += "/usr/X11R6/lib/psi";
 	dirs += "";
 	Stream::loadSSL(dirs);
 
