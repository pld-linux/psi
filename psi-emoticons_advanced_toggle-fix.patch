--- psi-0.9.3/psi/src/userlist.cpp.orig	2005-01-27 16:11:20.733254008 +0100
+++ psi-0.9.3/psi/src/userlist.cpp	2005-01-27 16:15:28.060654528 +0100
@@ -463,7 +463,7 @@
 					s = plain2rich(s);
 				if ( doLinkify )
 					s = linkify(s);
-				if( option.useEmoticons && option.emoticonsInToolTip && !doLinkify )
+				if( option.useEmoticons && option.emoticonsInToolTip /*&& !doLinkify */)
 					s = emoticonify(s);
 				str += QString("<br><nobr><u>%1</u></nobr><br>%2").arg(head).arg(s);
 			}
@@ -487,11 +487,13 @@
 			QString head = QObject::tr("Last Status Message");
 			if(trim)
 				s = plain2rich(clipStatus(s, 200, 12));
-			else {
+			else
 				s = plain2rich(clipStatus(s, 200, 12));
-				if ( doLinkify )
-					s = linkify(s);
-			}
+			
+			if ( doLinkify )
+				s = linkify(s);
+			if( option.useEmoticons && option.emoticonsInToolTip )
+				s = emoticonify(s);
 			str += QString("<br><nobr><u>%1</u></nobr><br>%2").arg(head).arg(s);
 		}
 	}
